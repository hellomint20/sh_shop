<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.testDBMapper">
	<insert id="insertDB">
		insert into
			testDB(contents, datetime, url, platform)
		values(#{contents}, #{datetime}, #{url}, #{platform})
	</insert>
</mapper>